<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/editor_bg">

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="42dp"
        android:background="@color/toolbar_bg"
        android:scrollbars="none">
        <LinearLayout
            android:id="@+id/tabs"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"/>
    </HorizontalScrollView>

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@color/toolbar_bg"
        android:scrollbars="none">
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal">
            <Button android:id="@+id/newButton" android:layout_width="90dp" android:layout_height="match_parent" android:text="New"/>
            <Button android:id="@+id/openButton" android:layout_width="90dp" android:layout_height="match_parent" android:text="Open"/>
            <Button android:id="@+id/saveButton" android:layout_width="90dp" android:layout_height="match_parent" android:text="Save"/>
            <Button android:id="@+id/saveAsButton" android:layout_width="100dp" android:layout_height="match_parent" android:text="Save As"/>
            <Button android:id="@+id/findButton" android:layout_width="90dp" android:layout_height="match_parent" android:text="Find"/>
            <Button android:id="@+id/themeButton" android:layout_width="90dp" android:layout_height="match_parent" android:text="Theme"/>
        </LinearLayout>
    </HorizontalScrollView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/lineNumbers"
            android:layout_width="48dp"
            android:layout_height="match_parent"
            android:gravity="top|end"
            android:paddingTop="10dp"
            android:paddingEnd="8dp"
            android:background="@color/toolbar_bg"
            android:textColor="#777B86"
            android:textSize="14sp"
            android:fontFamily="monospace"/>

        <com.example.notepadplusandroid.SelectionAwareEditText
            android:id="@+id/editor"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="top|start"
            android:background="@android:color/transparent"
            android:textColor="@color/editor_fg"
            android:textColorHighlight="#55607C"
            android:textSize="14sp"
            android:fontFamily="monospace"
            android:padding="10dp"
            android:inputType="textMultiLine|textNoSuggestions"
            android:overScrollMode="always"
            android:scrollbars="vertical|horizontal"/>
    </LinearLayout>

    <TextView
        android:id="@+id/statusBar"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:gravity="center_vertical"
        android:paddingStart="10dp"
        android:background="@color/toolbar_bg"
        android:textColor="#B8BBC4"
        android:textSize="11sp"/>
</LinearLayout>
